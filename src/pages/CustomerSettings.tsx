import { Typography } from "@mui/material"

const CustomerSettings: React.FC = () => {
    return (
        <Typography component={"div"}>
            CustomerSettings menu
        </Typography>
    )
}

export default CustomerSettings